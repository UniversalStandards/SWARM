# Architecture at a glance

Conceptual modules:
- Runtime engine (scheduler + message routing)
- Agent interface (roles, lifecycle)
- Tool registry + adapters
- Memory (ephemeral + persistent + vector)
- Policy engine (pre-flight checks + approvals)
- Observability (traces, metrics, logs)
- Evaluation harness

Diagrams live in `visuals/diagrams/`.
