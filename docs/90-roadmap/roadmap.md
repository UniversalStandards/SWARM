# Roadmap

## Phase 0 — Unification (now)
    1. Standardize repo structure
    2. Add governance + CI skeleton
    3. Import legacy repos (subtree preferred)

## Phase 1 — Core Runtime
    1. Agent base interface + registry
    2. Message types + serialization
    3. Tool adapter interface + registry
    4. Minimal policy gate

## Phase 2 — Memory + Workflows
    1. Memory backends (in-memory, sqlite)
    2. Workflow spec + validator
    3. Example workflows

## Phase 3 — Observability + Eval
    1. OpenTelemetry traces
    2. Metrics and structured logs
    3. Evaluation harness and safety suite

## Phase 4 — Scaling
    1. Distributed runtime options
    2. UI dashboard
    3. Adapter marketplace pattern
