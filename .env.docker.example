# Docker Compose Environment Configuration
# Copy this file to .env before running docker-compose up
# DO NOT commit the .env file to version control

# =============================================================================
# AI Provider API Keys (REQUIRED for AI functionality)
# =============================================================================
OPENAI_API_KEY=your_openai_api_key_here
ANTHROPIC_API_KEY=your_anthropic_api_key_here
GOOGLE_API_KEY=your_google_api_key_here

# =============================================================================
# GitHub OAuth Configuration (OPTIONAL - for GitHub integration)
# =============================================================================
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# =============================================================================
# Application Security (REQUIRED)
# =============================================================================
# Generate random secrets using: openssl rand -base64 32
NEXTAUTH_SECRET=generate_a_random_string_at_least_32_characters_long
JWT_SECRET=generate_a_random_string_at_least_32_characters_long

# =============================================================================
# Database Security (REQUIRED)
# =============================================================================
# PostgreSQL password - used by both app and postgres service
# Generate a strong password using: openssl rand -base64 24
POSTGRES_PASSWORD=generate_a_secure_random_password_here

# =============================================================================
# Monitoring & Admin Tools Security (REQUIRED)
# =============================================================================
# Grafana admin password
GRAFANA_ADMIN_PASSWORD=generate_a_secure_grafana_admin_password

# pgAdmin default password (only used when admin profile is active)
PGADMIN_DEFAULT_PASSWORD=generate_a_secure_pgadmin_password

# =============================================================================
# Quick Setup Guide
# =============================================================================
# 1. Copy this file: cp .env.docker.example .env
# 2. Generate secure passwords:
#    - POSTGRES_PASSWORD: openssl rand -base64 24
#    - NEXTAUTH_SECRET: openssl rand -base64 32
#    - JWT_SECRET: openssl rand -base64 32
#    - GRAFANA_ADMIN_PASSWORD: openssl rand -base64 16
#    - PGADMIN_DEFAULT_PASSWORD: openssl rand -base64 16
# 3. Add your AI provider API keys
# 4. (Optional) Add GitHub OAuth credentials
# 5. Run: docker-compose up -d
# =============================================================================
